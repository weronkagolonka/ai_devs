export const tableNames = {
    USERS: "users",
    DATACENTERS: "datacenters",
    CONNECTIONS: "connections",
};

export const COMPLETION_QUERY =
    "Get DC_ID of active datacenters whose managers are inactive";

export const TASK_NAME = "database";
